{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "platformAutomerge": true,
  "argocd": {
    "managerFilePatterns": [
      "/^argocd-apps/.*\\.yaml$/"
    ]
  },
  "helmv3": {
    "postUpdateOptions": [
      "helmUpdateSubChartArchives"
    ]
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "Image tags pinned via a `# renovate:` annotation, where the chart supplies the repository and only the tag is overridden",
      "managerFilePatterns": [
        "/^apps/.*/values\\.yaml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+image:\\s+tag: (?<currentValue>\\S+)"
      ]
    }
  ],
  "packageRules": [
    {
      "description": "The in-cluster registry is only reachable from the cluster, so Renovate can neither authenticate nor list tags there. gymbros tags are bumped by CI on every commit anyway.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["/^\\d{1,3}(?:\\.\\d{1,3}){3}:\\d+//"],
      "enabled": false
    },
    {
      "description": "argocd Applications track this same repo at HEAD; there is no version to bump",
      "matchManagers": ["argocd"],
      "matchDatasources": ["git-tags"],
      "matchPackageNames": ["https://github.com/Clusterforgers/k3s-cluster-manifests.git"],
      "enabled": false
    },
    {
      "description": "Default: Do not automerge Helm charts",
      "matchDatasources": ["helm"],
      "groupName": "helm charts",
      "automerge": false
    },
    {
      "description": "Trust list: Automerge these specific Helm charts",
      "matchDatasources": ["helm"],
      "matchPackageNames": ["ingress-nginx", "cert-manager"],
      "automerge": true
    },
    {
      "description": "Docker: Automerge minor and patch updates",
      "matchDatasources": ["docker"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    },
    {
      "description": "Nextcloud only supports upgrading one major at a time and runs occ migrations on every bump, so all updates are reviewed by hand. Must stay below the Docker automerge rule to override it.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["docker.io/library/nextcloud"],
      "automerge": false
    },
    {
      "description": "Immich releases (minors included) have carried breaking database migrations (v3.0.0 dropped pgvecto.rs), so all updates are reviewed by hand. Must stay below the Docker automerge rule to override it.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["ghcr.io/immich-app/immich-server"],
      "automerge": false
    },
    {
      "description": "A Postgres major is a data migration (pg_upgrade or dump/restore), not an image swap - swapping the tag leaves the new binary pointed at an old data directory and the pod will not start. Immich's own reference compose pins the 14 line.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["ghcr.io/immich-app/postgres"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "Traefik: Update on Monday",
      "matchPackageNames": ["traefik"],
      "schedule": ["before 5am on monday"]
    }
  ]
}
